# 数据库连接 (SQLite)
DATABASE_URL="file:./data/prod.db"

# NextAuth 配置 - 生产环境必须更改为强随机密钥
# 生成方法: openssl rand -base64 32
NEXTAUTH_SECRET="请替换为一个安全的随机字符串"

# 网站访问地址 (含协议和端口，无尾部斜杠)
NEXTAUTH_URL="https://your-domain.com"

# 服务端口 (可选，默认3000)
PORT=3000
